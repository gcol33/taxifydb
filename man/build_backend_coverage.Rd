% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/register.R
\name{build_backend_coverage}
\alias{build_backend_coverage}
\title{Build the backend coverage table}
\usage{
build_backend_coverage(
  backbone_paths = NULL,
  output_dir = NULL,
  version = NULL,
  manifest_path = "manifest/manifest.json",
  verbose = TRUE
)
}
\arguments{
\item{backbone_paths}{Named list/character vector or \code{NULL}. Explicit
\code{.vtr} path overrides for one or more of \code{\link[=register_backbones]{register_backbones()}}.
Backbones not named here resolve from \verb{output/<name>/<name>.vtr} (a
prior \code{\link[=build_backend]{build_backend()}} run) or from the version published in
\code{manifest.json}.}

\item{output_dir}{Character or \code{NULL}. Output directory. Default
\code{output/backend_coverage}.}

\item{version}{Character or \code{NULL}. Version string for the build. If
\code{NULL}, defaults to the current \code{YYYY.MM}.}

\item{manifest_path}{Character. Path to \code{manifest.json}, used to resolve
backbones not present locally.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
Path to \code{backend_coverage.vtr} (invisibly).
}
\description{
For each backbone in \code{\link[=register_backbones]{register_backbones()}} (resolved via
\code{resolve_register_backbone_paths()}), extracts the genus list and writes a
long-format \code{backend_coverage.vtr} recording which genera are covered by
which backend and at what version/date.
}
