% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_enrichment.R
\name{build_enrichment}
\alias{build_enrichment}
\title{Build an enrichment \code{.vtr} from source}
\usage{
build_enrichment(
  name,
  output_dir = NULL,
  version = NULL,
  url = NULL,
  resolve_names = TRUE,
  strict_names = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{name}{Character. Enrichment identifier. See \code{list_enrichments()}.}

\item{output_dir}{Character or \code{NULL}. Output directory. Default
\verb{output/enrichment/<name>}.}

\item{version}{Character or \code{NULL}. Version override. If \code{NULL}, uses the
registry's pinned version.}

\item{url}{Character or \code{NULL}. Custom source URL override. If supplied,
the build version defaults to \code{format(Sys.Date(), "\%Y.\%m")} and
\code{source_doi} is set to \code{NULL}.}

\item{resolve_names}{Logical. Run \code{\link[=resolve_enrichment_names]{resolve_enrichment_names()}} before
writing. Default \code{TRUE}.}

\item{strict_names}{Logical. Error when a backbone has no
\code{name_lookup.vtr}, instead of expanding against a narrower set and
warning. \code{FALSE} by default so a user building an enrichment locally
against whatever they have installed still works; the asset-publishing
pipeline sets \code{TRUE}, since a published asset must not be silently
expanded against a partial backbone set.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
Path to the built \code{.vtr} file (invisibly).
}
\description{
Downloads the raw source for an enrichment, parses it via the registered
parser, expands names across all 7 taxify backbones via
\code{\link[=resolve_enrichment_names]{resolve_enrichment_names()}}, and writes a \code{.vtr} plus \code{meta.json} sidecar
via \code{\link[=build_enrichment_vtr]{build_enrichment_vtr()}}.
}
