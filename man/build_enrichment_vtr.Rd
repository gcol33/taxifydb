% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_enrichment_vtr.R
\name{build_enrichment_vtr}
\alias{build_enrichment_vtr}
\title{Write an enrichment .vtr file}
\usage{
build_enrichment_vtr(
  df,
  vtr_path,
  name,
  version,
  source_url,
  source_doi = NULL,
  upstream_id = NULL,
  license = "unknown",
  attribution = NULL,
  group_col = NULL,
  species_col = NULL,
  static = TRUE,
  source_format = NULL,
  provenance = NULL,
  resolved_backbones = NULL,
  batch_size = 50000L
)
}
\arguments{
\item{df}{A data.frame with at least a \code{canonical_name} column.}

\item{vtr_path}{Character. Output path for the .vtr file.}

\item{name}{Character. Enrichment identifier (e.g., "zanne").}

\item{version}{Character. Version string (e.g., "2026.04").}

\item{source_url}{Character. URL the source data was downloaded from.}

\item{source_doi}{Character or NULL. DOI of the source dataset.}

\item{upstream_id}{Character or NULL. The identity the source host gives the
version this build read -- a Zenodo record number, a Figshare or Dryad
version number, a \code{Last-Modified} stamp. Recorded so the weekly freshness
check compares upstream against upstream; see \code{\link[=probe_upstream_identity]{probe_upstream_identity()}}.}

\item{license}{Character. License string (e.g., "CC0", "CC BY 4.0").}

\item{attribution}{Character. Human-readable attribution string.}

\item{group_col}{Character or NULL. Column to index for group-based
enrichments (e.g., "country_code", "tdwg_code", "lang").}

\item{species_col}{Character or NULL. Name of the source column the taxa were
keyed on, recorded for the runtime manifest. \code{"genus"} marks a genus-grain
enrichment; \code{NULL} (the default) is species-grain.}

\item{static}{Logical. Whether the enrichment is a frozen snapshot (the
default) rather than one the runtime re-checks against a live source. Drives
taxify's refresh gate (content-id for static, version for non-static).}

\item{source_format}{Character or NULL. Raw source format (e.g. "csv",
"xlsx", "zip"), recorded for the runtime manifest.}

\item{provenance}{Named list or NULL. Per-column provenance: maps each trait
column to its provenance tier, recorded in \code{meta.json}. Used by assets whose
columns come from mixed sources (the BETSI-recovery matrices); \code{NULL} (the
default) writes no provenance block.}

\item{resolved_backbones}{Character vector or NULL. The backbones the
cross-backbone name expansion actually reached. Recorded so an asset built
against a partial backbone set is distinguishable afterwards from a
complete one, which is what a bare warning at build time did not achieve.}

\item{batch_size}{Integer. Row group size for vectra (default 50000).}
}
\value{
The path to the .vtr file (invisibly).
}
\description{
Sorts by \code{canonical_name}, writes the .vtr, creates hash indexes on
\code{canonical_name} (and optionally a group column), and writes a
\code{meta.json} sidecar.
}
