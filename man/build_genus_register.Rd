% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/register.R
\name{build_genus_register}
\alias{build_genus_register}
\title{Build the genus register from a fixed backbone set}
\usage{
build_genus_register(
  backbone_paths = NULL,
  output_dir = NULL,
  version = NULL,
  manifest_path = "manifest/manifest.json",
  verbose = TRUE
)
}
\arguments{
\item{backbone_paths}{Named list/character vector or \code{NULL}. Explicit
\code{.vtr} path overrides for one or more of \code{\link[=register_backbones]{register_backbones()}}.
Backbones not named here resolve from \verb{output/<name>/<name>.vtr} (a
prior \code{\link[=build_backend]{build_backend()}} run) or from the version published in
\code{manifest.json}.}

\item{output_dir}{Character or \code{NULL}. Output directory. Default
\code{output/genus_register}.}

\item{version}{Character or \code{NULL}. Version string for the build. If
\code{NULL}, defaults to the current \code{YYYY.MM}.}

\item{manifest_path}{Character. Path to \code{manifest.json}, used to resolve
backbones not present locally.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
Path to \code{genus_register.vtr} (invisibly).
}
\description{
Reads genus-rank rows from each of \code{\link[=register_backbones]{register_backbones()}} (resolved via
\code{resolve_register_backbone_paths()}), unions them, resolves classification
conflicts, normalizes non-standard kingdom names, assigns \code{kingdom_group} /
\code{taxon_group} / \code{life_form} (via \code{assign_life_form()}, falling back to a
GBIF parent-key hierarchy walk for genera still unresolved), and writes
\code{genus_register.vtr}.
}
\details{
Every call unions the same fixed backbone set, so the built register is
reproducible: it does not depend on which backbones happen to be installed
on the machine running the build (contrast taxify's runtime
\code{taxify_build_register()}, which unions whichever backbones the caller has
installed).
}
