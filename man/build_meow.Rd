% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-meow.R
\name{build_meow}
\alias{build_meow}
\title{Build the MEOW ecoregion boundary \code{.vtr}}
\usage{
build_meow(
  output_dir = NULL,
  version = NULL,
  source_path = NULL,
  verbose = TRUE
)
}
\arguments{
\item{output_dir}{Character. Output directory. Default: \code{output/meow}.}

\item{version}{Character or NULL. Version string. Defaults to the current
\code{YYYY.MM}.}

\item{source_path}{Character or NULL. A local GeoJSON to parse instead of
downloading (for offline builds and testing).}

\item{verbose}{Logical.}
}
\value{
Path to the built \code{.vtr} (invisibly).
}
\description{
Downloads the MEOW ecoregion GeoJSON snapshot (or reads a local copy),
parses it to a long-format vertex table, and writes \code{meow.vtr} with an index
on \code{code} and a metadata sidecar. taxify downloads this pre-built \code{.vtr} and
rebuilds the boundary geometry offline, so the runtime never fetches the
GeoJSON. The marine analogue of \code{\link[=build_wgsrpd]{build_wgsrpd()}}.
}
