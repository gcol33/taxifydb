% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/register.R
\name{build_register}
\alias{build_register}
\title{Build both genus_register.vtr and backend_coverage.vtr}
\usage{
build_register(
  backbone_paths = NULL,
  version = NULL,
  manifest_path = "manifest/manifest.json",
  verbose = TRUE
)
}
\arguments{
\item{backbone_paths}{Named list/character vector or \code{NULL}. Explicit
\code{.vtr} path overrides for one or more of \code{\link[=register_backbones]{register_backbones()}}.
Backbones not named here resolve from \verb{output/<name>/<name>.vtr} (a
prior \code{\link[=build_backend]{build_backend()}} run) or from the version published in
\code{manifest.json}.}

\item{version}{Character or \code{NULL}. Version string for the build. If
\code{NULL}, defaults to the current \code{YYYY.MM}.}

\item{manifest_path}{Character. Path to \code{manifest.json}, used to resolve
backbones not present locally.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
Named list with paths to \code{genus_register.vtr} and
\code{backend_coverage.vtr} (invisibly).
}
\description{
Convenience wrapper that calls \code{\link[=build_genus_register]{build_genus_register()}} and
\code{\link[=build_backend_coverage]{build_backend_coverage()}} in sequence. Resolves every backbone's \code{.vtr}
exactly once (via \code{resolve_register_backbone_paths()}, downloading into
\verb{output/genus_register/_cache} when needed) and passes the fully resolved
paths to both builders as \code{backbone_paths}, so a manifest-downloaded
backbone (several hundred MB each, ~5 GB across the set) is fetched once
for both artifacts rather than once per builder.
}
