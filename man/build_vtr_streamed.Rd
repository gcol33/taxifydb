% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_vtr_streamed.R
\name{build_vtr_streamed}
\alias{build_vtr_streamed}
\title{Build a backbone .vtr from a stream of normalized chunks}
\usage{
build_vtr_streamed(
  feed,
  vtr_path,
  backend_name,
  version,
  source_url,
  synonym_pattern = "SYNONYM",
  batch_size = 50000L,
  work_dir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{feed}{A function of no arguments returning the next chunk of rows as a
normalized data.frame (the schema \code{\link[=normalize_backbone]{normalize_backbone()}} produces), or
\code{NULL} when the source is exhausted.}

\item{vtr_path}{Character. Output path for the .vtr file.}

\item{backend_name}{Character. Backend identifier (e.g. \code{"colxr"}).}

\item{version}{Character. Version string.}

\item{source_url}{Character. URL the source data was downloaded from.}

\item{synonym_pattern}{Character. Regex matching the statuses that count as
a synonym, as passed to \code{\link[=precompute_backbone]{precompute_backbone()}}.}

\item{batch_size}{Integer. Row group size for vectra (default 50000).}

\item{work_dir}{Character. Directory for the staging store. Defaults to a
session temporary directory, removed on exit.}

\item{verbose}{Logical.}
}
\value{
The path to the .vtr file (invisibly).
}
\description{
The streaming counterpart to \code{\link[=build_vtr]{build_vtr()}}, for a source too large to
assemble in memory. Rows arrive a chunk at a time from \code{feed}; the finished
store is identical to what \code{\link[=build_vtr]{build_vtr()}} would have written from the
concatenation of those chunks.
}
