% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-wgsrpd.R
\name{build_wgsrpd}
\alias{build_wgsrpd}
\title{Build the WGSRPD Level 3 boundary \code{.vtr}}
\usage{
build_wgsrpd(
  output_dir = NULL,
  version = NULL,
  source_path = NULL,
  verbose = TRUE
)
}
\arguments{
\item{output_dir}{Character. Output directory. Default: \code{output/wgsrpd}.}

\item{version}{Character or NULL. Version string. Defaults to the current
\code{YYYY.MM}.}

\item{source_path}{Character or NULL. A local GeoJSON to parse instead of
downloading (for offline builds and testing).}

\item{verbose}{Logical.}
}
\value{
Path to the built \code{.vtr} (invisibly).
}
\description{
Downloads the WGSRPD Level 3 GeoJSON (or reads a local copy), parses it to a
long-format vertex table, and writes \code{wgsrpd.vtr} with an index on \code{code}
and a metadata sidecar. taxify downloads this pre-built \code{.vtr} and rebuilds
the boundary geometry offline, so the runtime never fetches the GeoJSON.
}
