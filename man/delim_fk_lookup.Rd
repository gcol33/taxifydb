% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_vtr_streamed.R
\name{delim_fk_lookup}
\alias{delim_fk_lookup}
\title{Build an id -> value lookup for the ids a file's foreign keys reference}
\usage{
delim_fk_lookup(
  path,
  id_col,
  value_col,
  key_cols,
  chunk_rows = 1000000L,
  sep = "\\t",
  quote = "",
  encoding = "UTF-8",
  na_strings = c("", "NA"),
  col_names = NULL,
  verbose = TRUE
)
}
\arguments{
\item{path}{Character. Path to the delimited file.}

\item{id_col}{Character. Column holding the row's own identifier.}

\item{value_col}{Character. Column whose value the keys resolve to.}

\item{key_cols}{Character vector of foreign-key columns.}

\item{chunk_rows}{Integer. Rows to read per scan chunk.}

\item{sep, quote, encoding, na_strings, col_names}{As in \code{\link[=delim_chunk_feed]{delim_chunk_feed()}}.}

\item{verbose}{Logical.}
}
\value{
A named character vector of \code{value}, named by \code{id}.
}
\description{
A backbone that denormalizes its higher classification stores foreign keys
rather than names, so normalizing a row needs a name that lives in some other
row. GBIF is the case: kingdom, phylum, class, order and family all arrive as
\verb{*_key} columns pointing at rows that the build then drops.
}
\details{
The whole id -> name map would be as large as the backbone, but only the ids
actually referenced are ever looked up, and those are the few thousand rows
at the ranks the keys point to. Two scans find them without holding the map:
the first collects the distinct keys, the second keeps the \code{id}/\code{value} pair
of each row a key names. What comes back is small enough to sit beside a
chunk, which makes the rest of the normalization row-local.

The result is exactly the lookup a whole-table self-join would produce; it is
restricted by which ids are referenced, never by an assumption about the rank
a key points at.
}
