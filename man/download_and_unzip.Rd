% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment-helpers.R
\name{download_and_unzip}
\alias{download_and_unzip}
\title{Download a ZIP and extract, returning the path to a matching file}
\usage{
download_and_unzip(url, dest_dir, pattern = NULL)
}
\arguments{
\item{url}{Character. URL to a ZIP archive.}

\item{dest_dir}{Character. Directory for download and extraction.}

\item{pattern}{Character or \code{NULL}. Regex to match the target file inside
the ZIP. If \code{NULL}, returns the extraction directory itself.}
}
\value{
Path to the matched file, or the extraction directory if \code{pattern}
is \code{NULL}.
}
\description{
Downloads \code{url} to \code{dest_dir/source.zip} (if not cached), extracts into
\verb{dest_dir/extracted/}, and returns the first file matching \code{pattern}. If
\code{pattern} is \code{NULL}, returns the extraction directory itself.
}
