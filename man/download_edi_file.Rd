% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment-helpers.R
\name{download_edi_file}
\alias{download_edi_file}
\title{Download a file from EDI's PASTA repository, authenticated}
\usage{
download_edi_file(url, dest_dir, filename, max_tries = 4L)
}
\arguments{
\item{url}{Character. URL to download.}

\item{dest_dir}{Character. Directory to save into. Created if missing.}

\item{filename}{Character. Output filename.}

\item{max_tries}{Integer. Download attempts before giving up. Large live
exports (e.g. the World Spider Trait database) intermittently drop the TLS
connection mid-transfer; each retry uses exponential backoff.}
}
\value{
Path to the downloaded file.
}
\description{
EDI has required authentication for all REST API access since 2026-07-30
(an anti-DoS/scraping measure; see edirepository.org/news/news-20260727.00).
Anonymous requests to \code{pasta.lternet.edu} now get a 403 ("Public Access is
not authorized"). This wraps \code{\link[=download_curl_file]{download_curl_file()}} with EDI's documented
auth: an API Access Key appended as a \verb{?key=} query parameter (the
supported mechanism for "coded workflows, automated scripts, and other
integrations", per \url{https://edirepository.org/resources/iam}), read from
the \code{EDI_API_KEY} env var. \code{EDI_EDI_TOKEN} (a raw \code{edi-token} session JWT,
from a logged-in browser) is an interactive fallback -- it expires in days,
so it is not suitable for a scheduled/CI rebuild.
}
