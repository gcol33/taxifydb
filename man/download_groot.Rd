% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment-parsers-traits.R
\name{download_groot}
\alias{download_groot}
\title{Download both GRooT files (species aggregate + full per-record version)}
\usage{
download_groot(url, dest)
}
\arguments{
\item{url}{Character. The aggregate ZIP URL (from the registry).}

\item{dest}{Character. Download directory.}
}
\value{
Named character vector \verb{c(aggregate = <csv>, full = <csv or NA>)}.
}
\description{
The aggregate drives every trait; the full version is needed to repair
specific_root_area (see \code{\link[=parse_groot]{parse_groot()}}). The full download is best-effort:
if it fails, the aggregate SRA is used as-is.
}
