% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment-helpers.R
\name{harvest_plazi_dwca}
\alias{harvest_plazi_dwca}
\title{Harvest the Plazi Darwin Core archives covering a taxon}
\usage{
harvest_plazi_dwca(taxon_key, dest_dir, query = NULL, max_datasets = 20000L)
}
\arguments{
\item{taxon_key}{Integer. GBIF backbone key of the taxon (10713444 is class
Collembola).}

\item{dest_dir}{Character. Directory to cache the archives in. Created if
missing.}

\item{query}{Character or \code{NULL}. Free-text term for the second discovery
pass, usually the taxon name.}

\item{max_datasets}{Integer. Safety bound on the number of datasets.}
}
\value{
Path to the directory holding the cached archives.
}
\description{
Plazi republishes each marked-up paper as its own GBIF checklist dataset, so
a taxon's treatments are spread over hundreds of small archives rather than
one bulk file. Discovery is the union of two GBIF dataset-search filters
because neither is complete alone: \code{taxonKey} depends on GBIF having indexed
the dataset's taxonomic coverage, and the free-text \code{query} depends on the
paper's title or abstract naming the taxon. For class Collembola the two
return 210 and 489 datasets and their union is 508.
}
\details{
Each dataset is resolved to its \code{DWC_ARCHIVE} endpoint and cached as
\verb{<dataset key>.zip}, so a re-run only fetches what is new.
}
