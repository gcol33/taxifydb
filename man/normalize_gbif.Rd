% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-gbif.R
\name{normalize_gbif}
\alias{normalize_gbif}
\title{Normalize one block of GBIF rows to the unified schema}
\usage{
normalize_gbif(df, higher, verbose = TRUE)
}
\arguments{
\item{df}{A data.frame of raw GBIF rows, named by \link{.gbif_col_names}.}

\item{higher}{Named character vector of canonical names by id, from
\code{\link[=gbif_higher_lookup]{gbif_higher_lookup()}}.}

\item{verbose}{Logical.}
}
\value{
A normalized data.frame ready for \code{\link[=precompute_backbone]{precompute_backbone()}}.
}
\description{
Split out of \code{\link[=read_gbif]{read_gbif()}} so the streaming build can apply it to a chunk at
a time. Everything here depends only on the row in hand once \code{higher} is
supplied, which is the whole reason that lookup is a separate argument: the
\verb{*_key} columns point at rows anywhere in the file.
}
