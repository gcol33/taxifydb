% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment-parsers-betsi.R
\name{parse_betsi}
\alias{parse_betsi}
\title{Parse the BETSI Collembola body-length export into per-species values}
\usage{
parse_betsi(path)
}
\arguments{
\item{path}{Character. Path to the BETSI body-length \code{.xlsx} (or a directory
containing it).}
}
\value{
data.frame with \code{canonical_name} and the \verb{betsi_body_length_*}
columns, one row per species.
}
\description{
Reads the Zenodo BETSI body-length export and reduces its per-source
measurements to one row per Collembola species. \code{betsi_body_length_mm} is the
median of the recorded measurements (mm); \code{betsi_body_length_min_mm} /
\code{betsi_body_length_max_mm} bound them; \code{betsi_body_length_n} and
\code{betsi_body_length_sources} are the number of measurements and of distinct
literature sources behind the species' value.
}
