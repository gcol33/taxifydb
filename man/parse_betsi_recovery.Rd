% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/betsi-recovery.R
\name{parse_betsi_recovery}
\alias{parse_betsi_recovery}
\title{Parse a per-taxon BETSI-recovery matrix into per-species trait columns}
\usage{
parse_betsi_recovery(enrichment, path)
}
\arguments{
\item{enrichment}{Character. A per-taxon recovery enrichment name (see
\code{\link[=list_betsi_recovery]{list_betsi_recovery()}}).}

\item{path}{Character. Directory holding the source \code{.csv} files (staged from
\verb{inst/extdata/betsi/} by the build), or a single \code{.csv} when the enrichment
has one source.}
}
\value{
data.frame with \code{canonical_name} and the trait columns, one row per
species.
}
\description{
Reads the frozen matrices for the sources feeding \code{enrichment} and returns one
row per species. Fuzzy sources (\verb{species,trait,class,pct}) are checked for the
fuzzy-coding invariant (each species-by-trait affinity block sums to 100,
tolerating +-1 from integer rounding) and pivoted to one numeric column per
\verb{<trait>__<modality>} bin (0-100). Hard sources (one value per trait) are read
into one column per trait. Provenance is not a data column here; it is written
to \code{meta.json} from the descriptor by the build.
}
\seealso{
\code{\link[=build_betsi_recovery]{build_betsi_recovery()}}
}
