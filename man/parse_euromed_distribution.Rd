% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment-parsers-euromed.R
\name{parse_euromed_distribution}
\alias{parse_euromed_distribution}
\title{Parse the Euro+Med distribution snapshot}
\usage{
parse_euromed_distribution(path)
}
\arguments{
\item{path}{Character. Path to \code{euromed_distribution.jsonl}.}
}
\value{
data.frame with \code{canonical_name}, \code{area_code}, \code{area_name},
\code{area_level}, \code{iso2}, \code{euromed_status}, \code{euromed_status_detail},
\code{euromed_status_source} and \code{taxon_id}, with a reference table
(\code{ref_id}, \code{citation}, \code{doi}) attached by \code{\link[=attach_references]{attach_references()}}.
}
\description{
Reads \code{euromed_distribution.jsonl} and emits one row per (accepted taxon,
Euro+Med area): the status class, the term as the portal states it, the area
with its ISO 3166-1 alpha-2 country code where the area is one country, the
taxon's CDM UUID and the ids of the references behind the reported status.
A retracted report (\verb{reported in error}) is dropped; a former presence is
\code{extinct}. Where records of one taxon-area disagree the status follows
\code{.euromed_status_order} and the references are those stating it.
}
\details{
Areas whose name is not in \code{.euromed_area_iso} and is not a combined area or
part of a country are reported so the table can be extended.
}
