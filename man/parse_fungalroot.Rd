% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment-parsers-traits.R
\name{parse_fungalroot}
\alias{parse_fungalroot}
\title{Parse the FungalRoot database (GBIF Darwin Core Archive) to genus-level types}
\usage{
parse_fungalroot(path)
}
\arguments{
\item{path}{Character. Directory holding the extracted FungalRoot DwC-A.}
}
\value{
data.frame keyed on \code{genus} (also copied to \code{canonical_name}) with
\code{mycorrhizal_type}, \code{mycorrhizal_status}, and \code{mycorrhizal_records}.
}
\description{
Reads the FungalRoot occurrence core (\code{occurrences.csv}) and its
MeasurementOrFact extension (\code{measurements.csv}), keeps the per-observation
\verb{Mycorrhiza type} measurements, standardizes them to a small set of type
tokens, and reduces them to one row per plant genus by
majority consensus. Mycorrhizal type is phylogenetically conserved at the
genus level, which is the resolution FungalRoot itself recommends for
inference; the per-genus value here is the most frequent standardized type
across that genus's observations (taxifydb's own aggregation, not
FungalRoot's published per-genus assignment).
}
