% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment-parsers-gidias.R
\name{parse_gidias}
\alias{parse_gidias}
\title{Parse the GIDIAS impact database into per-species EICAT/SEICAT aggregates}
\usage{
parse_gidias(path)
}
\arguments{
\item{path}{Character. Path to the GIDIAS machine-readable \code{.csv} (or a
directory containing it).}
}
\value{
data.frame with \code{canonical_name}, \code{affected_taxon}, and the
\verb{gidias_*} aggregate columns.
}
\description{
Reads the GIDIAS machine-readable impact records and reduces them to
per-species environmental- and socio-economic-impact indicators. The \code{.vtr}
carries these derived aggregates; for the individual impact records, use the
GIDIAS figshare download.
}
\details{
\code{gidias_eicat_category} is the species' EICAT category: the most severe
magnitude among its negative (harmful) environmental-impact records, mapped
from the GIDIAS 0-3 magnitude scale to the IUCN levels MC/MN/MO/MR/MV (the
per-record global-extinction flag splits magnitude 3 into local MR and global
MV); a species with negative records but no scored magnitude is \code{"DD"}.
\code{gidias_seicat_category} applies the same rule to the constituents-of-
well-being block (SEICAT, MC/MN/MO/MR). The remaining columns summarise the
evidence: the driving mechanism(s), affected well-being constituents, the
species' functional group and realms, and record/source counts.

Nature's Contributions to People (NCP) is reported as a direction rather than
a category: \code{gidias_ncp_direction} gives the direction(s) recorded
(\code{"Negative"}, \code{"Positive"}, \code{"Neutral"}, or a combination for the 16\% of
species with a mixed record) and \code{gidias_ncp_affected} the contributions
affected. GIDIAS scores no magnitude scale for this block, so no IUCN-style
category is derived; the two columns are not paired, so use the source
records where the direction of a specific contribution matters.

Each species is aggregated twice. \code{affected_taxon = "Any"} summarises every
record, and is the grain to use unless you want one affected group. The
other rows summarise the species' impacts on one affected native taxon
(\code{"Plant"}, \code{"Invertebrate"}, \code{"Vertebrate"}, \code{"Microbe"}, \code{"Fungi"}), which
28\% of species split across two or more. Only the \code{"Any"} row carries SEICAT
and the negative records with no affected taxon recorded: the affected-taxon
axis slices the environmental-impact block, so the SEICAT columns are \code{NA} on
a per-taxon row.
}
