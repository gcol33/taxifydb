% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment-parsers-gift.R
\name{parse_gift}
\alias{parse_gift}
\title{Parse GIFT species-level plant traits (via the GIFT package)}
\usage{
parse_gift(path = NULL, agreement = 0.66, batch_size = 12L, verbose = TRUE)
}
\arguments{
\item{path}{Ignored; the GIFT package fetches data directly. Present so the
interface matches the file-based parsers.}

\item{agreement}{Numeric in \verb{[0, 1]}. Minimum agreement among source records
for a categorical value to be reported. Default \code{0.66}.}

\item{batch_size}{Integer. Number of trait IDs per API request. Default \code{12}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
data.frame keyed on \code{canonical_name} with one \verb{gift_<trait>} column
per GIFT trait. Numeric traits are doubles; the rest are character.
}
\description{
Downloads GIFT's full catalogue of species-level traits from the live API
(batched over all trait IDs) and pivots them to one row per species. GIFT
aggregates records to a single value per species (mean for numeric traits,
most frequent entry for categorical ones). Only the redistributable subset
the API returns is kept; restricted references are excluded by the default
call. The species name is GIFT's \code{work_species}.
}
