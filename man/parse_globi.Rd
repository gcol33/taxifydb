% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment-parsers-crawl.R
\name{parse_globi}
\alias{parse_globi}
\title{Parse the GloBI distinct-edge snapshot into per-species interaction degree}
\usage{
parse_globi(path)
}
\arguments{
\item{path}{Character. Path to \code{globi_edges.tsv.gz}.}
}
\value{
data.frame with \code{canonical_name}, \code{interaction_degree},
\code{n_interaction_types}, \code{n_interaction_records}.
}
\description{
Reads the crawled distinct (source, type, target) edges, resolves both
endpoints to their cross-backbone accepted names, and counts, per accepted
species, the number of distinct interaction partners (undirected), distinct
interaction types, and total interaction records. Degree is a distinct-count,
so it is aggregated at the accepted grain (both endpoints resolved) rather
than per raw name; the registry entry therefore sets \code{resolve_names = FALSE}.
}
