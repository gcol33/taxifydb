% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment-parsers-invacost.R
\name{parse_invacost}
\alias{parse_invacost}
\title{Parse the InvaCost economic-cost database into per-species aggregates}
\usage{
parse_invacost(path)
}
\arguments{
\item{path}{Character. Path to \code{InvaCost_database_v4.1.xlsx}.}
}
\value{
data.frame with \code{canonical_name}, \code{cost_total_usd} (2017 USD),
\code{cost_n} (number of cost estimates), \code{cost_type} (dominant cost type).
}
\description{
Reads the InvaCost v4.1 workbook and reduces the individual cost estimates to
three per-species economic-impact indicators. Only these derived aggregates
are distributed; the raw estimate rows are not.
}
\details{
\code{cost_total_usd} is the InvaCost-convention cumulative cost: each estimate's
standardised annual cost (\code{Cost_estimate_per_year_2017_USD_exchange_rate}, in
2017 USD at the exchange rate) is expanded across the number of years its
documented impact period spans (\code{Probable_ending_year_adjusted} -
\code{Probable_starting_year_adjusted} + 1, floored at one year where the period is
a single year or unrecorded) and summed across every estimate for the species.
Temporally or spatially overlapping estimates are not de-duplicated (the
\code{invacost} package's \code{summarizeCosts()} is the tool for a rigorous cost
analysis); \code{cost_total_usd} is a coarse economic-impact indicator, not an
audited figure. \code{cost_type} is the merged cost type (damage / management /
mixed / unspecified) that accounts for the largest share of the species'
cumulative cost.
}
