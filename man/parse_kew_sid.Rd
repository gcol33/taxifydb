% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment-parsers-kew.R
\name{parse_kew_sid}
\alias{parse_kew_sid}
\title{Parse SER-SID (Kew Seed Information Database) tables to per-species traits}
\usage{
parse_kew_sid(path)
}
\arguments{
\item{path}{Character. Directory holding the per-table RDS files written by
the registry download function (\code{species}, \code{seed_weights},
\code{storage_behaviour}, \code{oil_content}, \code{protein_content}, \code{morphology}).}
}
\value{
data.frame with \code{canonical_name} and the trait columns
\code{thousand_seed_weight} (grams per 1000 seeds, median),
\code{n_seed_weight_records}, \code{storage_behaviour}, \code{oil_content_pct},
\code{protein_content_pct}, \code{lifeform} (Raunkiaer code), and \code{fruit_type}.
}
\description{
Reduces the per-record SER-SID tables (many measurements per species) to one
row per species: seed weight and nutrient contents by median, storage
behaviour and fruit type by mode, with a record count for the seed-weight
evidence.
}
