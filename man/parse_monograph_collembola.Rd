% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment-parsers-monograph.R
\name{parse_monograph_collembola}
\alias{parse_monograph_collembola}
\title{Aggregate the frozen monograph body-length extraction to one row per species}
\usage{
parse_monograph_collembola(path)
}
\arguments{
\item{path}{Character. Path to the frozen extraction \code{.csv}, or a directory
containing one \code{.csv}.}
}
\value{
data.frame with \code{canonical_name} and the \verb{monograph_body_length_*}
columns, one row per species.
}
\description{
Reads the long-format frozen extraction (\code{canonical_name}, \code{source}, and the
\verb{body_length_*} columns; one row per species per monograph, as
\code{\link[=parse_monograph_body_length]{parse_monograph_body_length()}} produces for each book) and aggregates across
the source monographs to one row per species. This is the build reader for the
\code{monograph_collembola_body_length} enrichment: the source monographs are
copyrighted, so the mining runs once and only the extracted values -- facts,
not the book text -- are frozen and redistributed. \code{monograph_body_length_mm}
is the median of the per-monograph medians, \code{monograph_body_length_min_mm} /
\code{monograph_body_length_max_mm} bound the recorded values,
\code{monograph_body_length_n} is the total measurement count and
\code{monograph_body_length_sources} the number of monographs behind the value.
}
