% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment-parsers-traits-9.R
\name{parse_noddb}
\alias{parse_noddb}
\title{Parse NodDB, the global database of root-symbiotic nitrogen fixation}
\usage{
parse_noddb(path)
}
\arguments{
\item{path}{Character. Path to the downloaded \code{.xlsx}.}
}
\value{
data.frame with \code{canonical_name} (genus) plus nodulation columns.
}
\description{
\verb{NodDB database v1.3b.xlsx} from the PlutoF deposit behind Tedersoo et al.
(2018, Journal of Vegetation Science 29:560-568). Nodulation is recorded per
plant genus, not per species, so this is a genus-keyed source in the same
shape as FungalRoot: \code{canonical_name} carries the genus and taxify joins it
on \code{genus}.
}
\details{
The sheet puts its citation on row 1 and the header on row 2, so the header
is read positionally rather than by \code{read.csv}'s first-row assumption.

\verb{Consensus estimate} is the authors' verdict and is kept verbatim. The
canonical \code{nodulation_type} collapses it to the symbiont actually involved.
The collapse is an exact lookup rather than a regex because the vocabulary
contains \code{Rhizobia}, \code{likely_Rhizobia} and \code{unlikely_Rhizobia}, each of which
contains the one before it -- a pattern match in the wrong order would read
every negative verdict as a positive one. \verb{unlikely_*} is the authors'
judgement that the genus does not nodulate and reads as \code{none}.
}
