% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment-parsers-traits-6.R
\name{parse_thermofresh}
\alias{parse_thermofresh}
\title{Parse the Freshwater thermal-tolerance database (ThermoFresh)}
\usage{
parse_thermofresh(path)
}
\arguments{
\item{path}{Directory holding the extracted archive (with a \verb{data/}
subdirectory) or a path directly to that \verb{data/} directory.}
}
\value{
data.frame with canonical_name + freshwater thermal-limit traits.
}
\description{
Per-test critical thermal limits (Bayat et al.) for freshwater fish,
invertebrates and amphibians. Each source row is one tolerance test; the
processed tests are joined to the taxonomy table on \code{tax_id}, restricted to
species-level taxa, and reduced to one row per species (median per metric).
The retained metrics are the critical thermal maximum (\code{ctmax}), minimum
(\code{ctmin}), the median lethal temperature (\code{lt50}) and the lethal maximum /
minimum (\code{ltmax}, \code{ltmin}); all values are in degrees Celsius.
}
