% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publish.R
\name{publish_enrichment_release}
\alias{publish_enrichment_release}
\title{Publish enrichment \code{.vtr} files to the rolling enrichment release}
\usage{
publish_enrichment_release(
  version,
  vtr_paths,
  repo = "gcol33/taxifydb",
  notes = NULL
)
}
\arguments{
\item{version}{Character. Rolling release version (the \verb{enrichment-<version>}
tag).}

\item{vtr_paths}{Character vector. Paths to the enrichment \code{.vtr} files to
upload. Basenames become the release asset names.}

\item{repo}{Character. GitHub repo (e.g. "gcol33/taxifydb").}

\item{notes}{Character or NULL. Release notes, used only when the release is
created for the first time.}
}
\value{
The release tag (invisibly).
}
\description{
Every enrichment is published under one shared, rolling release tag
(\verb{enrichment-<version>}), unlike backbones which get a tag each. The release
is therefore created only when missing and never deleted: assets are uploaded
with \code{--clobber}, which replaces only the same-named file and leaves every
other enrichment's \code{.vtr} in place. Deleting and recreating the tag (as the
per-backbone workflow does) would wipe all the other enrichments' assets.
}
