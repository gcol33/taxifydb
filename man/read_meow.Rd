% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-meow.R
\name{read_meow}
\alias{read_meow}
\title{Parse the MEOW ecoregion GeoJSON into a long-format vertex table}
\usage{
read_meow(path)
}
\arguments{
\item{path}{Character. Path to a MEOW ecoregion GeoJSON file.}
}
\value{
A data.frame with columns \code{code}, \code{geom}, \code{ring}, \code{seq}, \code{lon},
\code{lat}, sorted by \code{code}, \code{geom}, \code{ring}, \code{seq}.
}
\description{
One row per polygon vertex. \code{code} is the MEOW ecoregion code (ECO_CODE),
\code{geom} indexes the polygon within a feature (a MultiPolygon has several),
\code{ring} indexes the ring within a polygon (\code{1} is the outer boundary, \verb{2+}
are holes), and \code{seq} is the vertex order within a ring. This captures the
full polygon topology so the runtime can rebuild native, terra, or sf
geometry from the same source, exactly like \code{\link[=read_wgsrpd]{read_wgsrpd()}}.
}
\details{
The ecoregion code property is matched case-insensitively, preferring
\code{ECO_CODE}; falling back to \code{ECO_CODE_X}, then any \code{eco*code} property.
}
