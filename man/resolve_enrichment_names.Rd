% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve_names.R
\name{resolve_enrichment_names}
\alias{resolve_enrichment_names}
\title{Resolve enrichment names against all taxify backends}
\usage{
resolve_enrichment_names(
  df,
  group_cols = NULL,
  backends = list_backends(),
  verbose = TRUE,
  use_lookup = TRUE
)
}
\arguments{
\item{df}{A data.frame with at least a \code{canonical_name} column.}

\item{group_cols}{Character vector of grouping columns. Deduplication uses
\code{canonical_name} + \code{group_cols} as the key. Default \code{NULL}.}

\item{backends}{Character vector of backend names. Default: every taxify
backbone (\code{\link[=list_backends]{list_backends()}}).}

\item{verbose}{Logical.}

\item{use_lookup}{Logical. Try the hash-join fast path first. Default \code{TRUE}.}
}
\value{
The expanded data.frame.
}
\description{
Takes an enrichment data.frame with \code{canonical_name} + trait columns and
expands it so that each source name maps to all unique \code{accepted_name}
values across the requested backends.
}
\details{
By default tries the fast hash-join path against per-backend
\code{name_lookup.vtr} files in the user's taxify data directory (built by
\code{\link[=build_all_name_lookups]{build_all_name_lookups()}}); falls back to per-name-per-backend
\code{\link[taxify:taxify]{taxify::taxify()}} if no lookup files are found.
}
