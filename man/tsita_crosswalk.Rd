% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tsita-vocab.R
\name{tsita_crosswalk}
\alias{tsita_crosswalk}
\title{The enrichment-to-T-SITA crosswalk}
\usage{
tsita_crosswalk(enrichment = NULL)
}
\arguments{
\item{enrichment}{Character or \code{NULL}. Restrict to one or more enrichment
names; \code{NULL} (default) returns the whole crosswalk.}
}
\value{
A data.frame with columns \code{enrichment}, \code{column}, \code{raw_value}
(\code{NA} for an axis mapping), \code{tsita_label}, \code{tsita_uri}.
}
\description{
Loads \code{inst/extdata/tsita_crosswalk.csv} and resolves each \code{tsita_label} to
its concept URI against \code{\link[=tsita_vocab]{tsita_vocab()}}, failing loudly if a label is not a
known T-SITA concept. Each row maps one enrichment column onto a T-SITA
concept: a row with an empty \code{raw_value} maps the column (the trait axis); a
row with a \code{raw_value} maps one categorical value of that column onto an
attribute concept.
}
\seealso{
\code{\link[=tsita_vocab]{tsita_vocab()}}
}
