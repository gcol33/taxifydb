% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tsita-vocab.R
\name{tsita_vocab}
\alias{tsita_vocab}
\title{The T-SITA thesaurus as a flat concept table}
\usage{
tsita_vocab()
}
\value{
A data.frame with columns \code{uri}, \code{id}, \code{prefLabel}, \code{top}, \code{depth},
\code{broader}, \code{definition}, \code{scopeNote}.
}
\description{
Loads the frozen T-SITA vocabulary (\code{inst/extdata/tsita_vocab.csv}): one row
per concept, with its stable ARK URI, \code{prefLabel}, the top concept it sits
under (\code{Trait} or \code{Ecological_preference}), its \code{depth} below that top, the
\code{broader} parent URI, and the \code{definition} / \code{scopeNote}. \code{prefLabel} is
unique across the thesaurus, which is what lets the crosswalk key on labels
and resolve URIs from here. The result is cached for the session.
}
\seealso{
\code{\link[=tsita_crosswalk]{tsita_crosswalk()}}
}
