% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publish.R
\name{update_enrichment_manifest}
\alias{update_enrichment_manifest}
\title{Update manifest.json enrichment entry from built meta.json}
\usage{
update_enrichment_manifest(
  manifest_path,
  name,
  vtr_path,
  release_version = NULL,
  repo = "gcol33/taxifydb"
)
}
\arguments{
\item{manifest_path}{Character. Path to manifest.json.}

\item{name}{Character. Enrichment identifier.}

\item{vtr_path}{Character. Path to the built .vtr file.}

\item{release_version}{Character or NULL. Rolling release version used in the
download URL tag (\verb{enrichment-<release_version>}). Defaults to the dataset
version from the meta sidecar.}

\item{repo}{Character. GitHub repo for URL construction.}
}
\value{
The updated manifest (invisibly).
}
\description{
Enrichment \code{.vtr} files are published under a single rolling release tag
(e.g. \code{enrichment-2026.06}) regardless of the dataset's own version. The
manifest entry therefore records two distinct versions: \code{latest} holds the
rolling release version (matching the \verb{enrichment-<latest>} download tag)
while \code{source_version} holds the dataset's own version (from the meta
sidecar). Pass \code{release_version} to set the release tag explicitly; when
omitted it falls back to the dataset version (correct only when the two
happen to coincide).
}
