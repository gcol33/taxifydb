% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publish.R
\name{update_manifest}
\alias{update_manifest}
\title{Update manifest.json after a successful backbone build}
\usage{
update_manifest(
  manifest_path,
  backend_name,
  version,
  vtr_path,
  delta_path = NULL,
  delta_from = NULL,
  extras = character(0L),
  repo = "gcol33/taxifydb",
  source_url = NULL
)
}
\arguments{
\item{manifest_path}{Character. Path to manifest.json.}

\item{backend_name}{Character.}

\item{version}{Character.}

\item{vtr_path}{Character.}

\item{delta_path}{Character or NULL.}

\item{delta_from}{Character or NULL. Previous version the delta is from.}

\item{extras}{Character vector. Paths to sidecar artifacts uploaded with
the release. Recorded as \verb{extras: [\{name, url, size, sha256\}]} in the
manifest entry; the runtime downloader fetches each into the same
versioned directory as the main \code{.vtr}.}

\item{repo}{Character. GitHub repo for URL construction.}

\item{source_url}{Character or NULL. Original data source URL. When \code{NULL}
(the default), it is read from the \code{url} field of the \code{.meta} sidecar that
\code{build_vtr()} writes next to \code{vtr_path}, which is the backend's single
source of truth for provenance.}
}
\value{
The updated manifest (invisibly).
}
\description{
Update manifest.json after a successful backbone build
}
